/*import { useParams } from 'react-router-dom';
import FarmacoTest from '../../components/RecipeDoctor';
import ListaPrescricoes from '../../components/ListaPrescricoes';

const PrescricaoPage = () => {
	const { patientId } = useParams();

	if (!patientId) return <p>Paciente não encontrado.</p>;

	return (
		<div className='max-w-3xl mx-auto p-4'>
			<FarmacoTest patientId={patientId} />
			<ListaPrescricoes patientId={patientId} />
		</div>
	);
};

export default PrescricaoPage;
*/
